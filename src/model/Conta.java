package model;

abstract class Conta implements OperacoesBancarias {
    /*o coração do sistema, implementa a interface de Operacoes Bancarias porque é lá que diz
    * o que toda conta deve fazer, com exceção de sacar() porque o saque depende de cada conta*/

}
